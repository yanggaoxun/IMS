# 类生产启动：docker compose up -d --build
# 本地开发仍建议 artisan serve + npm run dev（HMR 不适用此容器）
services:
  app:
    build: .
    ports:
      - "8000:80"
    env_file:
      - .env
    restart: unless-stopped
